此文件夹用于统一存放「六个一 · 语文素材本」导出的文件（TXT / Word / 素材包 .js）。

浏览器下载默认会进入"下载"目录：
1. 点导出后，到浏览器下载目录找到刚下载的文件；
2. 把它移动（或剪切）到这个文件夹；
3. 若使用 Chrome/Edge 的"保存文件"对话框（新版程序会弹出），可直接选择本文件夹保存。

建议每周更新素材后，把导出的 sucai_日期.js 替换到 sixone/data/sucai.js，实现永久保存。
