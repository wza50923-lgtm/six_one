# 「六个一 · 语文素材本」执行计划

> 创建日期：2026-08-17 ｜ 状态：执行中 ｜ 总文件夹：D:\six_one

## 一、总体目标

做一个**双击即可在电脑上使用**的本地网页程序（`index.html` + 独立数据文件），用于记录高考语文"六个一"作文素材：**①格言名句 ②一首好诗 ③一节美文 ④素材积累**。界面简洁、可勾选、可编辑、支持自定义字数，一键导出 TXT 与真正的 Word（.docx）。可选接入 DeepSeek API，实现"按主题生成诗词解析""把新闻改写成议论分析"等 AI 辅助。

## 二、程序结构（HTML + 独立数据文件，已确认）

```
D:\six_one\
├─ 六个一执行计划.md       ← 本计划文档（用户要求写入总文件夹）
└─ sixone\
   ├─ index.html          主程序：界面 + 逻辑 + 导出（双击运行）
   ├─ css\style.css       简洁风格样式
   ├─ js\main.js          交互逻辑（勾选/展开/编辑/字数/预览/导出/AI）
   ├─ js\docx.js          内置轻量 .docx 生成器（纯函数，无外部依赖）
   ├─ data\
   │  ├─ geyan.js  格言数据（考纲必背篇目范围内，30 条）
   │  ├─ shici.js  好诗数据（2 首 + 100~200 字解析）
   │  ├─ meiwen.js 美文数据（真实满分作文片段，标注来源与链接）
   │  └─ sucai.js  时效素材（近一周，5 条，带日期与来源）
   └─ README.md    使用说明 + API Key 申请 + 安全 + 每周更新指南
```

**关键设计**：数据用 `.js` 文件（全局变量）而非 `.json`——`file://` 双击打开时浏览器禁止 fetch 本地 json，但允许加载本地 `<script>`。每周更新素材只需改 `data/sucai.js` 一个文件。

> **实现说明（与原计划的差异）**：原计划"内嵌 docx.js（第三方库）"改为**内置自研轻量 .docx 生成器**（js/docx.js，约 200 行）：构建环境无外网无法下载库文件，自研实现"无压缩 ZIP + WordprocessingML"，生成的仍是真正可被 Word/WPS 打开的 .docx，且程序更小、完全离线。

## 三、界面设计（简洁风格）

- **顶栏**：标题「六个一 · 语文素材本」、姓名/班级/日期输入、⚙ 设置（API Key）、导出 TXT / Word / 复制 / 打印。
- **主体四张卡片**，对应四类内容，每张卡片头部有"启用/勾选"开关，点开后展开子选项：
  1. **格言名句**：按适用主题筛选（立志奋斗/坚持积累/家国情怀/修身自省/求知学习/逆境豁达/创新变通），条目列表（出处 + 主题标签），可勾选多条；目标条数可自定义；可补充自定义格言。
  2. **一首好诗**：内置 2 首默认诗词（苏轼《定风波》、王维《山居秋暝》，含解析）；可选"按主题 AI 生成"（诗/词，字数上限与解析字数均自定义）。
  3. **一节美文**：展示真实满分作文段落（标注年份/题目/来源链接）；**不提供 AI 生成**（只收真实段落），可编辑、可粘贴补全，目标字数自定义并实时校验。
  4. **素材积累**：4~5 条素材卡片（标题 10~20 字 / 议论分析 50~100 字 / 类别 / 日期 / 来源），可勾选、可编辑；"获取近期热点"（免费热榜 API）+ "粘贴新闻 → AI 写标题和分析"。
- **底部**：成稿预览区（实时排版效果）+ 字数统计 + 一键复制/导出。
- 所有勾选与编辑自动存入浏览器 `localStorage`（键 `sixone.v1`），下次打开不丢失。

## 四、字数自定义（新增需求，所有生成内容均支持）

| 位置 | 自定义项 | 默认值 |
|---|---|---|
| 格言 | 目标条数 | 5 条 |
| 好诗·AI 生成 | 体裁（诗/词）、全诗字数上限、解析字数 | 词、80 字、150 字 |
| 好诗·内置 | 解析字数校验（显示实际字数） | 100~200 字 |
| 美文 | 目标字数（实时显示实际字数与差额） | 200 字 |
| 素材 | 标题字数（min~max）、分析字数 | 10~20 字、75 字 |
| 导出 | 按所选内容生成，附字数统计 | — |

所有字数输入会写入 AI 提示词并实时显示"实际/目标"徽章。

## 五、四类内容的准备方式（P1 检索结果）

| 类别 | 内容 | 准备方式 |
|---|---|---|
| 格言 | 30 条**高频**格言，取自高考必背篇目（《论语》《劝学》《师说》《赤壁赋》《岳阳楼记》《离骚》《逍遥游》《滕王阁序》等）及考纲常见名句 | 人工整理，每条标注出处 + 适用主题 |
| 好诗 | 苏轼《定风波·莫听穿林打叶声》（词）、王维《山居秋暝》（诗），各附 100~200 字解析 | 精选 + 撰写解析；AI 生成为备选 |
| 美文 | 2026 全国Ⅰ卷（广东）满分作文真实段落 | 联网检索，见下方"已知限制" |
| 素材 | 5 条，事件均发生在 2026-08-10 ~ 08-17（一周内） | 联网检索（新华社/央广网/人民日报/新华财经等） |

**2026 年广东高考作文题背景**：2026 全国Ⅰ卷（广东）作文题以"词语"为引，主题被解读为"词观万象，思见成长"（参考：[新浪·中大教授彭玉平拆解](http://k.sina.com.cn/article_2131593523_7f0d893302001nqqe.html)、[广州日报·作文题出炉](https://news.dayoo.com/guangdong/202606/07/139996_54967233.htm)）。

**美文已知限制（如实说明）**：检索工具只能获得搜索结果摘要，无法打开网页全文，因此内置的是**公开转载页面中可确认的原文片段**（如《一词见成长，读懂坚持的真正意义》开头段"岁月更迭，年少时以为坚持是咬牙硬扛的执拗……"），并附**原文来源链接**；程序内"美文"卡片支持一键打开来源、粘贴补全为完整段落。**绝对不用 AI 生成美文**（遵守"不要自己生成"的要求）。

## 六、AI 集成与安全（重点）

**安全方案：**
1. **Key 只存本机**：保存在浏览器 `localStorage`，**绝不写进任何文件**（HTML/JS 里不含 Key）。
2. **纯前端直连** `https://api.deepseek.com`（官方控制台同源方案，CORS 通常放行）；失败自动降级离线模式并提示。
3. 设置面板内置：**一键清空 Key**、费用提示（deepseek-chat 单次约几百 token，不足 1 分钱）、平台可设余额提醒。
4. 可选进阶：README 提供"本地代理"方案（Key 放服务端、浏览器只连 127.0.0.1）。
5. README 警示：不要在公共电脑使用、不要截图外发 Key、导出文档不含 Key。

**AI 功能边界（防幻觉、保真实）：**
- 好诗：提示词硬性限定体裁 + 字数上限 + 必须附指定字数解析；
- 素材：AI **不编造新闻**，只基于用户粘贴的新闻/热榜条目写标题与分析，并标注"分析由 AI 撰写"；
- 美文：完全禁用 AI 生成。

**时效素材的"浏览器查询"方案**：程序内置免费热榜 API（首选 `api.vvhan.com/hotlist`，备选 `60s-api.viki.moe`），点"获取近期热点"即拉取热点列表，选中后交给 AI 写分析；接口不可用时自动降级为"手动粘贴新闻"。每周素材更新也可由助手（本会话可联网检索）重写 `data/sucai.js`。

## 七、导出实现

- **TXT**：`Blob` 下载，UTF-8；排版：标题行 → 日期 → 「一、格言名句 / 二、好诗一首 / 三、美文一段 / 四、素材积累」分节 → 条目编号 → 字数统计。
- **Word**：内置生成器（js/docx.js）产出真正的 .docx：标题居中加粗、分节标题、正文首行缩进两字符、宋体/Times New Roman、页边距，Word/WPS 直接打开。
- 附赠：预览区支持 `Ctrl+P` 另存 PDF。

## 八、执行步骤（共 5 阶段）

1. **P1 内容检索与整理**（已完成）：检索近一周新闻素材、广东满分作文、整理格言与好诗。
2. **P2 数据文件**：`data/*.js` 四类数据（含字数自定义默认值）。
3. **P3 界面与交互**：`index.html` + `css/style.css` + `js/main.js`：四卡片、勾选/展开、编辑、字数徽章、预览、localStorage。
4. **P4 AI 与热榜**：API Key 设置面板（含安全提示）、DeepSeek 调用、热榜接口 + 降级、错误处理。
5. **P5 导出与验收**：TXT + .docx 导出、离线验证、README 编写。

## 九、风险与对策

| 风险 | 对策 |
|---|---|
| 查不到"广东专属"满分作文全文 | 内置真实片段 + 来源链接 + 粘贴补全；明确标注网络流传版 |
| 素材过期（超两周） | 每周让助手更新 `sucai.js`；程序内热榜 API 兜底 |
| 浏览器直连 DeepSeek 被 CORS 拦截 | 实测降级 + README 提供本地代理脚本方案 |
| API Key 泄露/被盗用 | 仅存本机 + 一键清空 + 平台限额提醒 + README 警示 |
| 热榜 API 失效 | 双接口轮询 + 手动粘贴降级 |

## 十、你需要做的事

1. 确认方案（已确认并开始执行）；
2. 去 [platform.deepseek.com](https://platform.deepseek.com) 申请 API Key（充值 5~10 元足够用很久；没有 Key 不影响离线使用）；
3. 每周对助手说"更新素材"，刷新时效内容。

---


## 附录：执行记录（2026-08-17 完成）

### 产出文件

```
D:\six_one\
├─ 六个一执行计划.md      ← 本文件
└─ sixone\
   ├─ index.html          主程序（双击运行）
   ├─ css\style.css       简洁风格样式
   ├─ js\main.js          交互逻辑
   ├─ js\docx.js          内置轻量 .docx 生成器
   ├─ data\geyan.js       格言 30 条（7 大主题）
   ├─ data\shici.js       好诗 2 首（定风波/山居秋暝，解析 152/154 字）
   ├─ data\meiwen.js      美文 2 篇（2026 全国Ⅰ卷=广东卷 满分作文真实片段+来源链接）
   ├─ data\sucai.js       素材 5 条（2026-08-15 ~ 08-17，均在时效内）
   └─ README.md           使用/安全/每周更新指南
```

### 已实现功能

- 四卡片勾选/展开/编辑，localStorage 自动保存（键 sixone.v1）；
- 字数自定义：格言目标条数、诗/词 AI 体裁与字数上限、解析字数、美文目标字数、素材标题与分析字数，全部实时徽章校验；
- 成稿预览 + 一键导出 TXT / Word（内置生成器产出合法 .docx）/ 复制 / 打印；
- DeepSeek API：Key 仅存本机（设置面板一键清空）、直连官方接口、测试连接、失败降级提示；
- 素材 AI 助手：粘贴新闻→标题+分析（不编造事实）；好诗 AI 生成（限长+附解析）；
- 热榜查询：api.vvhan.com 与 60s-api 双接口轮询 + 手动粘贴降级；
- 数据文件用 .js（file:// 下可加载），每周更新只需替换 data/sucai.js。

### 验证结果（本机离线验证）

- 6 个 JS 文件语法检查全部通过；
- 数据完整性：格言 30 条/主题 7 类，好诗解析 152/154 字（100~200 达标），素材 5 条均在 2026-08-10~17 时效内；
- .docx 生成器：ZIP 结构（4 条目、本地头/CRC/大小）逐字节校验通过，document.xml 标签配对、中文、XML 转义全部通过；
- 界面与交互冒烟测试：启动渲染、预览、AI 生成（好诗/素材）、TXT/Word 导出、自动保存均通过；
- index.html 与 main.js 引用的 45 个元素 id 交叉检查无缺失。

### 已知限制（如实记录）

- 美文为公开转载页可确认的原文片段（非全文），附来源链接，可在程序内粘贴补全；绝无 AI 生成；
- 热榜 API 在 file:// 下可能被浏览器跨域拦截，已提供手动粘贴降级；
- AI 生成内容（诗/素材分析）需用户自行审定后使用。

### 后续每周更新

对助手说“更新素材”即可重新联网检索近一周新闻并重写 data/sucai.js（程序自动并入新条目，不覆盖你的勾选与编辑）。
## 附录 V2（同日追加）：网页内一键更新素材

- 新增「🔄 一键更新素材」：程序自拉当天热点（60s 新闻 / vvhan 热榜，免费接口）→ DeepSeek 按自定义字数生成标题+分析 → 存本机并自动勾选，**无需回助手处更新**；
- 新增「⬇ 导出素材包(.js)」：把当前素材导出为可替换 data/sucai.js 的文件，便于换电脑 / 永久保存；
- 验证：一键更新全链路（热榜→AI→JSON 解析含代码块剥离）、无 Key 守卫提示、素材包导出，冒烟测试全部通过。
## 附录 V3（同日追加）：按新需求升级

- 格言 / 好诗 / 美文均支持「AI 补充」：格言按主题补真实名句；**好诗只检索真实名家诗词（不创作）**＋每首可「✍️解析」让 AI 按自定义字数生成解析；美文补充**完整段落**并注明来源性质（AI整理版会明确标注）；
- 素材增加「要素」字段（时间/主体/关键数据，要求具体不空泛）：内置 5 条与 AI 生成均含要素，预览/导出一并呈现；
- 所有条目旁均有删除按钮；内置条目删除采用**墓碑机制**（删除后刷新不复活，除非「清空本机记录」）；
- 新增「导出文件」文件夹（D:\six_one\导出文件\），导出时优先弹出“保存文件”对话框便于直接存入该文件夹；
- 修复：AI 补充诗词的标题/作者解析正则转义问题；
- 验证：AI 补充×4、AI 写解析、一键更新含要素、删除墓碑重载、导出含要素、index.html 与 main.js id 交叉检查，全部通过。
## 附录 V4（同日追加）：第二批升级

- 素材新增「梗概」字段（标题下方，约30字，含六要素：时间/地点/人物/起因/经过/结果）：内置 5 条已补（32~39字），AI 写分析 / 一键更新 / 不满意重新生成均会生成梗概，预览与导出一并呈现；
- 名句与诗词**过滤课内内容**：内置格言换成 20 条课外著名名言（罗曼·罗兰、泰戈尔、尼采、鲁迅《热风》、史铁生、王国维《人间词话》、钱钟书《围城》、顾城、刘慈欣《三体》等）；内置好诗换成课外经典（苏轼《临江仙·夜归临皋》、王维《终南别业》）；AI 补充提示词加入“不来自小学/初中/高中课本与高考必背篇目”的硬性要求；
- 素材支持**批量挑选**：☑全选 / ☐全不选 / ⇄反选；
- 美文与素材每条旁新增「🔄不满意」按钮：AI 按当前主题/要素重新生成（美文重写完整段落，素材重写标题/梗概/要素/分析）；
- 验证：6 文件语法 OK；梗概/批量/重新生成/一键更新（含梗概）/导出含梗概/渲染控件/id 交叉全部通过（美文重生成曾因测试桩路由误判，修正 mock 后确认应用逻辑正确）。
## 附录 V5（同日追加）：细节调整

- 素材条目**删除**“要素/关键词”输入行（30字梗概下方），梗概已含六要素，界面更简洁；数据/导出/AI 提示词同步去除要素字段；
- 「🔄不满意」升级为**填写要求 + 重新生成**：每条美文/素材下方新增修改要求输入框（如：换个角度、更贴合××主题、更有文采），填写后点击按钮，AI 按你的要求重新生成（要求会随条目保存）；
- 验证：渲染无要素行/有要求输入行、素材与美文重生成均使用填写的要求、AI 分析与一键更新无要素字段、导出含梗概无要素，全部通过。
## 附录 V6（同日追加）：第三批升级

- **展开/收起**：文字按钮改为 **SVG 箭头**（旋转动画），卡片展开收起带 **max-height 流畅过渡**；
- **美文**：移除 AI 整理版，内置改为**公开转载页面确认的真实原文片段**＋来源链接；AI 补充改为「推荐真实篇目与获取方式」（不生成正文）；每篇「🔄换推荐」；
- **获取热点**：优先使用 **DeepSeek Responses API（内置 web_search 联网搜索）**（POST /responses，tools 声明 web_search，兼容解析 output_text/output/choices，失败自动回退 60s/vvhan 热榜）；每条热点**标明精确日期与媒体来源**；
- **热点多选**：热点列表可勾选多条 →「⚡ 批量生成素材」一次处理；
- **一键更新**：更新素材不删除旧条目；新增「💾 保存到项目文件夹」：用 File System Access API 直接写入 sixone/data/sucai.js（不支持时回退导出下载）；
- **AI 补充诗词**：新增主题/作者**建议 chips**（含 🎲 随机），解析支持「不满意填写要求→重写」；
- 修复：String.replace 的 $$ 转义陷阱导致 $$ 选择器写回失败（改用 split/join）；read 工具大文件截断导致的语法误判（测试改分块读取）；
- 验证：展开动画、chips 填入、解析带要求重写、美文推荐、热榜联网（日期/来源）、多选批量、一键更新、保存到文件夹，全部通过。
## 附录 V7（同日追加）：细节修复

- **生成素材置顶**：AI 写分析 / 一键更新 / 热点批量生成的素材一律插入列表**最顶端**并自动展开素材卡片、平滑滚动到新条目（此前追加在底部，且 max-height 冻结值 + overflow:hidden 会导致内容被裁剪、无法滚动查看）；
- **展开高度释放**：展开动画结束后把 max-height 释放为 none（不再冻结高度），彻底解决内容变高后被裁剪且无法滚动的问题；收起前先取真实高度再归零，保证动画流畅；
- **预览不显示网址**：成稿预览与 TXT/Word 导出中，素材来源只显示媒体名称（如“来源：新华社”），网址仅保留在编辑界面；
- **诗歌逐字保证**：内置诗为通行版本原文；AI 补充诗词提示词强制【正文】逐字一致、严禁改动/润色/扩写/二次创作，无法确认原文时**拒答不入库**并提示用户自贴权威版本；
- 验证：置顶顺序（单条/一键/批量）、展开高度释放（none/0px）、预览无 URL、拒答逻辑，全部通过。
## 附录 V8（同日追加）：一键生成 / 去重 / 移动端

- **诗歌、格言后生成置顶**：AI 补充的格言、诗词与素材一样，新条目一律插入列表最上方；
- **顶部「✨ 一键生成」**：一次生成 5 条不同类型格言 + 1 首真实诗词（逐字原文+解析，无法确认原文则拒答跳过）+ 5 条一周内素材，全部置顶勾选、走原有四卡片界面编辑；
- **生成历史去重**：state.usedTitles 记录每次生成的素材标题与热点标题（上限 300 条，随 localStorage 持久化）；一键生成/一键更新/热点批量自动排除历史内容，状态栏提示"已排除 N 条"；
- **手机/平板适配**：新增 <640px 与 641~1024px 两档媒体查询（头部纵向堆叠、按钮全宽、卡片换行、输入框 16px 防 iOS 缩放、热点多选换行、预览内边距缩小等），功能不变；
- 验证：一键生成三步链路（格言5置顶/诗词1置顶/素材5置顶）、二次生成去重（历史热点被排除）、AI 补充诗词置顶，全部通过。
## 附录 V9（同日追加）：交互细节

- **勾选与展开解耦**：卡片头部复选框只控制启用/禁用（禁用卡片半透明），**展开/收起只由右侧箭头控制**（勾选/取消勾选都不改变展开状态）；
- **顶部「✕ 取消全部选择」**：一键清空四类内容的所有勾选；
- **一键生成自动勾选**：生成的格言5/诗词1/素材5 全部默认勾选，无需手动逐个勾选；
- **打印优化**：Ctrl+P 只打印「成稿预览」卡片（隐藏顶部栏、四张卡片、弹窗等整个 UI）；
- **完成动画**：一键生成结束后，页面中央弹出**居中卡片式完成动画**（对勾描边 + 弹出动效，约 4 秒自动消失，可点「继续编辑」关闭），不铺满全屏；
- 验证：勾选不展开/箭头展开/取消勾选保持展开、取消全部选择、一键生成 11 项全部勾选、完成动画显示与关闭、打印样式只输出预览，全部通过。
## 附录 V10（同日追加）：一键生成体验修复

- **完成动画不再变淡/挡操作**：done-overlay 背景改为 transparent + pointer-events:none（界面保持正常选择与显示，动画卡片本身可点「继续编辑」关闭）；
- **一键生成后自动滚动到素材本**：完成后 scrollIntoView 到「成稿预览」卡片（新增 id="card-preview"），勾选的新素材在下方素材本清晰可见；
- 排查确认：素材列表、成稿预览、勾选状态数据均正常（此前看不到为遮罩变淡 + 未滚动所致）；
- 验证：预览含"四、素材积累"与新素材、卡片展开、勾选 5 条、遮罩透明不拦截，全部通过。
## 附录 V11（同日追加）：一键生成与素材本显示修复

- **删除完成动画**：一键生成完成后不再弹出任何动画/浮层（HTML/CSS/JS 全部移除），界面无打扰；
- **删除透明/变淡效果**：移除完成遮罩的半透明背景，也移除卡片禁用态的半透明样式（card.disabled），界面不再变淡；
- **修复素材本不显示的 bug（根因）**：若此前素材卡片被取消勾选过，enabled=false 会被保存，导致成稿预览整段跳过素材。修复：
  1. 成稿预览改为**只要该节有勾选项就显示**（不再受卡片启用开关阻断）；
  2. 一键生成开始时**强制启用 格言/好诗/素材 三卡**，之后生成的内容必进素材本；
- 验证：enabled=false 且有勾选时预览仍显示"四、素材积累"+新素材；一键生成后三卡自动启用、11 项自动勾选、无任何动画残留引用；全部通过。
